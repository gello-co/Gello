#!/usr/bin/env sh
# Auto-update private ops repo on branch switches; never fail
if [ -x ./.ops/scripts/ops-ensure.sh ]; then
  ./.ops/scripts/ops-ensure.sh || true
elif [ -f ./.ops/scripts/ops-ensure.sh ]; then
  bash ./.ops/scripts/ops-ensure.sh || true
fi
