# Development Test User Credentials
# Copy this file to .env.local and customize if needed
# These credentials match the seeded test users in scripts/seed-db.ts

# Admin user credentials
ADMIN_EMAIL=ada.admin@example.com
ADMIN_PASSWORD=password123

# Manager user credentials
MANAGER_EMAIL=alice.manager@example.com
MANAGER_PASSWORD=password123

# Member user credentials
MEMBER_EMAIL=ivy.member@example.com
MEMBER_PASSWORD=password123

# Common password for all test users (if using DEV_PASSWORD instead of individual passwords)
DEV_PASSWORD=password123
