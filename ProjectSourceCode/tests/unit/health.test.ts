import { describe, expect, it } from "vitest";

// Placeholder test while backend tests are being set up.
// Keep CI green without importing Bun-specific modules.
describe("placeholder", () => {
	it("passes trivially", () => {
		expect(true).toBe(true);
	});
});
