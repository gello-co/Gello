name: Release Placeholder

on:
  workflow_dispatch:
  pull_request:
    types: [closed]
    branches: [dev]

jobs:
  release-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Release automation temporarily disabled
        run: |
          echo "Release automation is temporarily disabled"
