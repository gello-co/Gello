[serve.static]
env = "BUN_PUBLIC_*"

[test]
# Load Supabase/Bun setup before any tests execute
preload = ["./happydom.ts", "./tests/setup/bun-setup.ts"]
# Integration tests need generous timeout for Supabase startup/network retries
timeout = 30000
