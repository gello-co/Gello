#!/bin/bash
# Post-start command: Runs every time container starts
# Currently empty - can be used for startup tasks if needed

set -e

cd "$(dirname "$0")/.."

# This script runs every time the container starts
# Add any startup tasks here if needed
